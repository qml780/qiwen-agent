# 本地音乐服务说明

## 当前状态

- 已安装到 `D:\游戏agent\ACE-Step-1.5`。
- 模型、缓存、临时文件和输出均位于 D 盘。
- 使用快速音乐模型，关闭额外语言模型；提示词由漆问对话 Agent 整理。
- 已针对 RTX 4060 Laptop 8GB 启用主模型 INT8、主模型常驻显存、小组件按需换入换出。
- 漆问网页通过本机 `127.0.0.1:8001` 调用，生成音乐不消耗外部音乐 API 额度。

## 启动

双击项目根目录的 `打开漆问.cmd`。启动脚本会依次启动：

1. 本地音乐生成服务；
2. 漆问 API；
3. 漆问网页。

网页界面不会显示服务、模型或 API 名称，只显示生成状态。

## 已验证结果

- 10 秒、48kHz、双声道 WAV 可真实生成并播放。
- 单版本生成通过。
- 一次生成两个不同版本通过。
- 生成结果可下载、校验并保存到漆问素材库。
- 修改提示词后会使用新的随机种子重新生成，不会重复返回固定敲击声。
- 本地音乐生成估算费用为 0 元。

## 运行日志

- 服务输出：`D:\游戏agent\ace-step-api.log`
- 服务错误：`D:\游戏agent\ace-step-api-error.log`
- 漆问 API：`D:\游戏agent\qiwen-api.log`
- 漆问 API 错误：`D:\游戏agent\qiwen-api-error.log`

## 注意事项

- 第一次生成需要加载模型，等待时间会长于后续生成。
- 生成时会占用约 6GB 显存。若 Unity 同时运行超大型场景，可先保存场景并减少其他占用显存的软件。
- 当前只生成音乐与游戏氛围配乐；语音和音效可后续作为独立素材类型接入。
