# 漆问开发智能体规则

1. 开始任何修改前，先读 `MASTER_SPEC.md`、本文件及任务相关技术文档；MASTER SPEC 优先级最高。
2. 产品是以漆艺知识为起点的人机共创游戏工作室，不是自动游戏生成器。前台始终只有一个“共创助手”。
3. 坚持“智能助手提议，玩家决定”。不得替玩家批准、跳阶段、发布、删除资产或执行未预览的 Unity 写操作。
4. 所有产物按版本保存；修改导致旧批准失效。服务端状态机和 Bridge 均必须验证当前版本批准，不能只依赖界面。
5. Provider 使用统一契约；无凭据时明确进入精选模拟模式，绝不把模拟结果标成真实调用。密钥只在 API 服务端。
6. 可见界面使用中文与黑白灰；工作区主导、聊天次要，不使用渐变、彩色状态或通用 AI SaaS 卡片堆叠。
7. Unity 写操作只通过 loopback Local Bridge 和窄工具；禁止任意 shell、C# eval、反射和任意路径。每次写入先预览、批准、检查点、回执。
8. DeepSeek 输出先做 schema 校验；代码只能先成为提议，且只可写受控生成区。自动修复最多三轮。
9. 修改后按风险运行 API、Web、Bridge、状态机及 Unity EditMode/PlayMode 测试；不得以模型陈述替代工具证据。
10. 用户素材原文件不得覆盖或删除；未核验权利的素材仅限内部精选演示。所有生成文件只可写入 D 盘或 E 盘。

