# 《漆问》全链路缺陷报告

更新时间：2026-08-19  
最高依据：`MASTER_SPEC.md`（未修改）

## 已修复并复验

| 编号 | 缺陷 | 根因 | 修复与证据 | 状态 |
|---|---|---|---|---|
| B-001 | “单独修改本阶段”显示 `Failed to fetch` | `knowledge` 不在 `APPROVAL_TRANSITIONS`，后端抛出 `KeyError` | `apps/api/app/main.py` 对知识阶段单独处理；API 测试覆盖后续阶段返回知识修改 | 已修复 |
| B-002 | 保存想法后没有进入概念 | 项目恢复和阶段入口不一致 | `apps/web/components/studio.tsx` 恢复当前项目；`POST /idea` 实测进入 `concept_drafting` | 已修复 |
| B-003 | 音乐生成提示本地服务不可用 | 总启动脚本没有启动 8001 音乐服务 | `scripts/start-qiwen.ps1` 直接执行 `start-music-service.ps1` 并等待 8001 | 已修复 |
| B-004 | 接口内部仍连接不到音乐和本地桥 | `httpx` 继承系统代理，本机 127.0.0.1 请求误走代理 | `bridge_client.py`、`asset_providers.py` 对本机调用设置 `trust_env=False`；`/health/dependencies` 全部正常 | 已修复 |
| B-005 | 不同音乐提示返回相同或只有敲击声 | 旧合成回退与固定结构不能证明音乐生成 | 本地音乐 Provider 真实生成两段 10 秒、48kHz WAV；SHA-256 分别为 `6040236C...89AD` 与 `4FDECDAF...9E09` | 已修复 |
| B-006 | 重做六项素材后仍显示旧“素材已批准” | 仅删除内存批准，数据库 `ApprovalRow` 未删除，重载后复活 | `game-assets/rework` 同步删除 3D/素材批准行；新增集成测试；真实项目重载只保留概念、视觉、音乐批准 | 已修复 |
| B-007 | 预算显示接近 30 元但供应商实际约 2.4 元 | 将估算费用记成实际费用并累加 | 实际确认值与估算账本分离；`/budget` 为实际 2.4、剩余 27.6、估算账本 29.3 | 已修复（实际值仍需人工同步） |
| B-008 | 视觉/素材只有一张合成图，无法逐项命名生成 | 旧流程把素材组当单资产 | 游戏素材阶段现显示 6 个独立命名条目、独立提示词、逐项生成/确认/删除；浏览器证据 `evidence/full-audit-工作台.png` | 已修复 |
| B-009 | Unity 只出现灰色碗，不是游戏 | 旧构建只验证导入与 Play Mode，没有玩法模板 | 新增 `topdown-dodge` 计划驱动模板；真实 Unity 构建任务 `36213cc7-6bd3-4075-bafe-2ec893566b80` 成功，运行时出现玩家、两类液滴、倒计时/碰撞/重开逻辑 | 已修复（最终美术未完成） |
| B-010 | 躲避玩法被误判成工序谜题 | 模板判断优先匹配知识文本中的“顺序” | `providers.py` 优先识别躲避、弹幕、四方向移动和不能攻击；真实逻辑第 6 版为 `topdown-dodge` | 已修复 |
| B-011 | 最终试玩反馈后项目自动完成 | 最终反馈接口直接写 `Stage.COMPLETED`，缺独立玩家批准 | 新增 `awaiting_final_approval` 和 `POST /projects/{id}/finish`；前台新增“完成项目”；62 项 API 测试通过 | 已修复 |
| B-012 | 网页规范检查产生八百余错误 | ESLint 扫描 `.next-m11` 旧构建产物，源码另有 Hook 命名/Effect 问题 | 排除 `.next*`，修正源码 Hook 与状态同步；最终 lint、typecheck、build 全通过 | 已修复 |
| B-013 | 深色主题部分音乐列表仍为白底 | 原始浅色规则与原生播放器未完全覆盖 | `globals.css` 增加全站深漆主题、音乐行和媒体控件深色样式；浏览器首页计算背景 `rgb(9,7,5)` | 已修复 |
| B-014 | 一键脚本新增音乐后子脚本报 `$PSScriptRoot` 为空 | 以 ScriptBlock 执行子脚本丢失脚本目录上下文 | 改为直接执行 `& $musicScriptPath`；完整冷启动通过 | 已修复 |

## 仍开放或外部阻断

| 编号 | 问题 | 当前证据 | 状态/处理 |
|---|---|---|---|
| O-001 | 当前图片无法真实生成 | 真实请求返回连接失败；本机到图像服务主机 443 无法建立 TCP；没有生成文件写入素材库 | BLOCKED：外部网络/服务连接；前台保留待生成，不使用回退冒充 |
| O-002 | 当前 6 项新美术尚未生成与批准 | 用户项目 `658be659-...` 的 6 项均为 `pending` | BLOCKED：依赖 O-001 和玩家逐项选择 |
| O-003 | 当前用户项目尚未用新素材最终构建 | 真实 Unity 已验证玩法模板，但使用的是旧批准测试资产 | PARTIAL：等新素材和逻辑批准后再构建 |
| O-004 | 三维真实生成未执行付费闭环 | 凭据配置存在，未有本轮 GLB 生成、摄取、Web/Unity 双端证据 | BLOCKED：外部付费调用与玩家是否选择 3D |
| O-005 | 月实际消费 2.4 元是人工确认快照 | `/budget` 明确 `cost_basis=供应商余额页确认值` | PARTIAL：后续应接供应商账单同步或提供手动更新界面 |
| O-006 | 完整浏览器 E2E 无法走到最终完成 | 图像阶段真实服务阻断；系统正确停止在素材阶段 | BLOCKED：禁止把 Mock/精选素材当本次生成 |
| O-007 | 发布、社区画廊不存在 | `MASTER_SPEC.md` 第 76 节明确 MVP 不做社区 | 不属于当前 MVP；评分已在试玩中实现，发布/画廊未实现也不冒充 |

## 当前测试基线

- API：62/62 通过。
- Web：3/3 通过；TypeScript、ESLint、Next.js 生产构建通过。
- Bridge：8/8 通过；TypeScript 构建通过。
- 浏览器：主页、知识库、项目、工作台无控制台错误或失败请求。
- 冷启动：数据库完全停止后，`D:\游戏agent\打开漆问.cmd` 成功恢复数据库、音乐、API、Web、Bridge；Unity/MCP 连接正常。

