# 素材系统

素材分为当前项目和个人素材库，类型为图像、三维、音频与代码引用。每项保存稳定编号、对象路径、哈希、MIME、大小、版本、Provider provenance、许可状态和项目引用。

重新生成创建新候选，不覆盖旧版；加入或移出项目只改变引用，不删除原文件。用户提供且权利未核验的素材标记为 `internal_demo_only`，不得进入公开发布物。

摄取校验包括：图像可解码与尺寸；GLB 容器、mesh、面数和材质；音频可解码、时长与循环信息。Web 与 Unity 必须引用同一 artifact version/hash。Unity 写入前展示资产、对象名、Transform 和交互差异，并保留检查点与回执。

