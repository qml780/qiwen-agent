# MILESTONE 6 验收报告

## 完成情况

- Web 已实现全中文、黑白为主的 Unity 构建监控：本地桥接、Unity 编辑器、工具通道、实时事件、当前动作、进度、编译/域重载、控制台、错误指纹与执行轮次。
- Bridge 提供 SSE 事件流与 `after` 序号回放；浏览器重连只读取任务，不会再次提交构建。
- 失败任务可受控重试，最多三轮；第四次请求返回冲突，不继续修改或消耗资源。
- 人工接管会中止自动化但保持可见 Unity 编辑器和当前工程，不删除现场。
- 任务、事件、请求与错误指纹持久化到 `D:\qiwen-runtime\bridge\jobs`；桥接重启后可恢复历史任务，执行中断任务会标记为可安全重试的失败。
- 浏览器保存当前任务引用，刷新页面后恢复同一任务的完整事件证据。
- 生成脚本在写入前计算 SHA-256 短指纹，并校验命名空间、类名、长度、禁用 API 与 D/E 路径白名单。

## 真实 Unity 验收

- 无人干预复跑任务：`2e67f176-3b1a-4d85-b8c5-24adf69b7ac3`。
- 结果：状态“成功”，第 `1 / 3` 轮，13 条有序事件，进度 100%。
- 耗时：2026-08-11T01:23:32.642Z 至 2026-08-11T01:23:40.695Z，约 8.1 秒。
- 脚本指纹：`aa2641bf65d6`。
- Unity 证据链：创建场景 → 导入 GLB → 导入 WAV → 创建对象 → 生成脚本 → 编译/域重载 → 挂载 → Play Mode → 完成。
- 最终结果：`D:\qiwen-runtime\unity-projects\QIWEN-VerticalSlice\QIWEN\Results\vertical-slice.json`，`success: true`。
- UI 证据：`D:\qiwen-runtime\evidence\m6-unity-monitor-success.png`。

## 测试与检查

- Bridge TypeScript 编译通过。
- Bridge 自动测试 5/5：令牌、路径边界、SSE 回放、断线无重复执行、三轮重试、人工接管。
- API Python 编译通过，自动测试 30/30。
- Web TypeScript 检查通过，自动测试 3/3。
- Edge 无头浏览器完成桌面 UI 实际渲染；连接均显示“已连接”，任务恢复后显示 100%、13 条控制台事件。
- Bridge 重启后已成功恢复上一真实任务，证明任务证据持久化有效。

## 联调中发现并修复

- 首次联调在 75% 等待编译：原因是恢复菜单在 Domain Reload 完成前调用。
- 修复为编译阶段每五秒重新探测 MCP，并调用幂等恢复入口；第二次真实复跑 8.1 秒自动完成。
- 修复 API 默认 Unity 项目仍指向慢速 E 盘的问题，正式改为 D 盘运行工作区。
- 修复 D 盘 Unity 项目路径被 Bridge 白名单拒绝的问题。
- 修复连接状态只检测一次的问题，现每五秒更新。
- 修复刷新页面丢失当前任务的问题，现从本地任务引用与 Bridge 持久状态恢复。

## 已知边界

- 当前自动修复轮次具备错误指纹、次数上限和受控生成区校验；真正由 DeepSeek 根据编译错误生成补丁的闭环在 M8/M9 接入，M6 不扩大 Agent 写权限。
- Coplay MCP 在 Play Mode 切换附近会短暂重连，健康状态轮询会自动恢复。
- UI 中供应商/格式等必要技术专名仍保留；产品动作、状态、错误与帮助文案均为中文。

## 决策

- M6 完成规则已满足，可以按用户连续授权进入 M7。
- M7 严格逐个实现五类模板，每个模板完成独立 Demo 与测试后才进入下一个。

