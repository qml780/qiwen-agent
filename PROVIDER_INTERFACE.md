# 生成服务接口

图像、三维和音乐 Provider 共享异步任务契约：`validate → submit → poll → ingest`，并支持 `cancel`、超时、错误分类、费用预估和幂等键。Mock 与 Real 返回相同任务/产物结构。

当前运行顺序与首选服务：聊天使用米醋 `gpt-5.6-terra`，结构化设计与 Unity 代码使用米醋 `gpt-5.6-sol`；图像使用米醋 `gpt-image-2-pro`，三维使用混元生 3D，音乐暂时使用可重复的精选模拟数据。远端成功结果必须先下载到 E 盘内容寻址存储，验证 MIME、大小、SHA-256 及媒体专用约束，再成为项目资产，不能依赖临时 URL。米醋图像接口返回 URL 或 base64 时都先摄取到本地对象存储。

全局月预算为 30 元，不设单项目硬预算。预计超额时暂停提交，并让玩家选择继续付费、改用模拟、取消或推迟；不得静默扣费。供应商名称、模型、任务编号、费用、许可与来源必须持久化。
