# 界面规范

- 语言：所有玩家可见文案使用中文；Unity 等必要产品专名可保留。
- 色彩：只使用黑、白、灰。禁止渐变、彩色霓虹、玻璃拟态和无意义装饰色。
- 信息层级：中央工作区为主；左侧素材、右侧共创助手为辅；顶部展示阶段，底部展示项目进度。
- 审批：当前阶段、当前版本、批准状态和下一动作必须同时可见。已批准阶段可以只读回看。
- 状态：加载、空内容、错误、重试、禁用、思考、构建、域重载、编译与人工接管均有明确中文反馈。
- 可访问性：`zh-CN`、键盘焦点、跳转链接、语义化错误/状态播报、媒体替代名称、减少动态效果。
- 响应式：桌面三栏；窄屏改为单栏，素材侧栏隐藏但核心流程保留；390 像素宽不得横向溢出。

